[package]
name = "bk4819"
version = "0.1.0"
edition = "2021"

authors = ["Aaron Griffith <aargri@gmail.com>"]

[dependencies]
bitfield-struct = "0.7"
defmt = { version = "0.3.5", optional = true }

[features]
defmt = ["dep:defmt"]
